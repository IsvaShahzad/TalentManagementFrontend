/** In-app toast visibility (top-right alerts). */
export const TOAST_DEFAULT_DURATION_MS = 5500;

/** Ignore duplicate toasts with the same text/color within this window. */
export const TOAST_DEDUPE_MS = 2500;

/** Ignore duplicate OS / socket notifications with the same id or message within this window. */
export const OS_NOTIFICATION_DEDUPE_MS = 8000;
